# My Project Monorepo

## Description
This project contains the mobile app, backend, and admin dashboard.

## How to Run
1. Install dependencies: `npm install`
2. Start Docker: `docker-compose up -d`
3. Run Backend: `cd backend && npm run dev`